{{#if nav}}
<nav class="nav">
  <h1 class="nav-title">
    <a href="/">Savannah &amp; Konstantin</a>
  </h1>

    <b class="nav-toggle nav-item">
        <a data-icon="a" href="#menu">menu</a>
    </b>

    <ul class="nav-list">
      {{#each nav}}
        <li class="nav-item {{#if isActive}}is-nav-item-active{{/if}}">
            <a href="{{url}}">{{label}}</a>
        </li>
      {{/each}}
    </ul>
</nav>
{{/if}}
